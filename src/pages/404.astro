---
import BaseLayout from "../layouts/BaseLayout.astro";
import Container from "../components/Container.astro";
import Button from "../components/Button.astro";

export const prerender = true;
---
<BaseLayout title="Page not found | Rivermark Group" description="Page not found.">
  <section class="py-24">
    <Container>
      <p class="text-sm uppercase tracking-[0.2em] text-ink-600">404</p>
      <h1 class="mt-4 text-4xl md:text-5xl">We can’t find that page.</h1>
      <p class="mt-4 max-w-xl text-lg text-ink-600">
        The page may have moved. Use the navigation to continue, or return home.
      </p>
      <div class="mt-8">
        <Button href="/">Back to home</Button>
      </div>
    </Container>
  </section>
</BaseLayout>
