---
const links = [
  { href: "/services", label: "Services" },
  { href: "/industries", label: "Industries" },
  { href: "/work", label: "Work" },
  { href: "/insights", label: "Insights" },
  { href: "/about", label: "About" }
];
---
<header class="sticky top-0 z-50 border-b border-white/80 bg-sand-100/80 backdrop-blur">
  <nav class="mx-auto flex w-full max-w-6xl items-center justify-between px-6 py-4">
    <a href="/" class="flex items-center gap-3 text-lg font-semibold text-ink-900">
      <span class="inline-flex h-9 w-9 items-center justify-center rounded-full bg-ink-900 text-white">
        R
      </span>
      Rivermark Group
    </a>
    <div class="hidden items-center gap-8 text-sm font-semibold text-ink-700 md:flex">
      {links.map((link) => (
        <a href={link.href} class="hover:text-ink-900">
          {link.label}
        </a>
      ))}
      <a
        href="/contact"
        class="rounded-full border border-ink-300 px-4 py-2 text-sm font-semibold text-ink-900 hover:border-ink-600"
      >
        Contact
      </a>
    </div>
    <details class="md:hidden">
      <summary class="cursor-pointer list-none rounded-full border border-ink-300 px-4 py-2 text-sm font-semibold text-ink-900">
        Menu
      </summary>
      <div class="mt-3 flex flex-col gap-3 rounded-2xl border border-white/70 bg-white/90 p-4 text-sm shadow-lift">
        {links.map((link) => (
          <a href={link.href} class="text-ink-800">
            {link.label}
          </a>
        ))}
        <a href="/contact" class="font-semibold text-ink-900">
          Contact
        </a>
      </div>
    </details>
  </nav>
</header>
